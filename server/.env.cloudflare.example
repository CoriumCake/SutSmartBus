# Copy this file to .env.cloudflare and replace the placeholder value.
#
# Create the token in Cloudflare Zero Trust:
# Networks -> Tunnels -> Create a tunnel -> Docker
# Then paste the generated token below.

CLOUDFLARE_TUNNEL_TOKEN=replace-with-your-cloudflare-tunnel-token
