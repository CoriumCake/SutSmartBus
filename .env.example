# Root environment file for docker-compose and GitHub Actions compatibility
# Copy to `.env` at the repository root for local overrides.

MQTT_BROKER_HOST=mosquitto
MQTT_BROKER_PORT=1883
MONGODB_URL=mongodb://mongodb:27017/sut_smart_bus
TZ=Asia/Bangkok
API_SECRET_KEY=
# Optional dedicated bell-signing secret. If set, this must match
# hardware/esp32_cam/config.h RING_COMMAND_SECRET.
RING_COMMAND_SECRET=
ADMIN_PASSWORD=admin123
CORS_ORIGINS=*
RATE_LIMIT_PER_MINUTE=60
MAX_UPLOAD_SIZE=2097152
OTA_FALLBACK_IP=203.158.3.14
DB_FILE=bus_passengers.db
