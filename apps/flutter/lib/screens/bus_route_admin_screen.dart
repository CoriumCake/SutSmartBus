import 'package:flutter/material.dart';

class BusRouteAdminScreen extends StatelessWidget {
  const BusRouteAdminScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Bus Route Admin Screen')));
  }
}
