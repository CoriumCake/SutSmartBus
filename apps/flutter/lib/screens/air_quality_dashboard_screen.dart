import 'package:flutter/material.dart';

class AirQualityDashboardScreen extends StatelessWidget {
  const AirQualityDashboardScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Air Quality Dashboard Screen')));
  }
}
